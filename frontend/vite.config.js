// Vite configuration removed — project migrated to Create React App.
// This file is intentionally left as a noop so tooling that expects it
// doesn't fail. The active frontend build system is now `react-scripts`.
export default {}
