// Legacy CRA file placeholder renamed/cleared to avoid Vite build parsing of JSX in .js
// The real app uses `App.jsx` in this project. This small file prevents accidental imports.
// Legacy Create-React-App file renamed to avoid Vite conflicts. Original content moved to App.js.bak
export default null;
